# Dependencies for SPADE depth estimation (PyTorch-based).
# Install into a separate venv from requirements.txt (which targets TensorFlow / SUIM-Net).
#
# On ARC Great Lakes, load these modules first:
#   module load python/3.10.4 cuda/11.8.0 cudnn/11.8-v8.7.0
# then:
#   pip install -r requirements_spade.txt

torch>=2.1.0
torchvision
numpy==1.24.*
# TensorFlow needed by SUIM-Net (danger map pipeline loads both models).
# typing_extensions must be pinned AFTER tensorflow to undo its downgrade.
tensorflow==2.13.1
keras==2.13.1
typing_extensions>=4.10.0
scipy>=1.10
matplotlib
pandas
h5py
scikit-learn
timm
tqdm
tifffile
einops
huggingface-hub
pyarrow
Pillow
opencv-python-headless<4.11
requests
pyyaml
